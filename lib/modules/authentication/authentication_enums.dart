enum AuthenticationStatus {
  unknown,
  authenticated,
  unauthenticated,
}

enum LoginMethod {
  google,
  apple,
  email,
  phone,
}

enum FormzSubmissionStatus {initial, failure, success, inProgress}
